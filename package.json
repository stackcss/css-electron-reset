{
  "name": "css-electron-reset",
  "description": "Reset Electron stylesheets so it behaves like a native application",
  "main": "index.css",
  "repository": "stackcss/css-electron-reset",
  "version": "1.0.0",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "start": "node .",
    "test": "standard && npm run deps"
  },
  "dependencies": {},
  "devDependencies": {
    "dependency-check": "^2.9.1",
    "standard": "^10.0.2",
    "tape": "^4.7.0"
  },
  "keywords": [
    "css",
    "electron",
    "reset"
  ],
  "license": "MIT"
}
